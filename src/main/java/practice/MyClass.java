package practice;

public class MyClass {
    int x;
    // You can also write this as public MyClass()
    // Or make it dynamic by writing it as public MyClass(int y)
    // If its dynamic I can say x=y; here and manually input 5 in the = new MyClass(5);
    public MyClass(int y) {
        x=5;
    }
}
